PHASE 0
Foundation

Goal:

Can we load and query 100k rows?

Nothing else.

Build:

- Postgres

Table:

queries
(
  id,
  query,
  historical_count
)

Only these fields.

Dataset loader:

Input:

query,count
iphone,100000

Output:

100k+ rows

Deliverables:

- Dataset Ingestion
- Schema
- Seed Script
- Architecture

Architecture:

Dataset
   ↓
Postgres

README Snapshot:

Version 1:
Data Storage Layer

Why this phase exists

Because everything depends on data.

Without data:

Cache?
Trending?
Batching?

meaningless.